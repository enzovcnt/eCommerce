<nav class="flex items-center justify-between flex-wrap bg-gray-500 p-6 sticky top-0 z-50 shadow-md">


    <div class="block lg:hidden">
        <button id="burger-btn" class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                <title>Menu</title>
                <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/>
            </svg>
        </button>
    </div>

    <div id="burger-menu" class="w-full hidden lg:flex lg:items-center lg:w-auto">
        <div class="text-xl lg:flex-grow">
            {% if app.user %}
                <a href="{{ path('app_cart') }}">Cart <strong>({{ cart_global.getCount() }})</strong></a>
                <a href="{{ path('app_products') }}" class="block mt-4 lg:inline-block lg:mt-0 text-yellow-200 hover:text-white mr-4 text-xl">Accueil</a>
                <a href="{{ path('app_product_create') }}" class="block mt-4 lg:inline-block lg:mt-0 text-yellow-200 hover:text-white mr-4 text-xl">Create</a>
                <a href="{{ path('app_category') }}" class="block mt-4 lg:inline-block lg:mt-0 text-yellow-200 hover:text-white mr-4 text-xl">Category</a>
                <a href="{{ path('app_address', {id:app.user.profile.id}) }}" class="block mt-4 lg:inline-block lg:mt-0 text-yellow-200 hover:text-white mr-4 text-xl">Address</a>
                <a href="{{ path('app_shopping', {id:app.user.profile.id}) }}" class="block mt-4 lg:inline-block lg:mt-0 text-yellow-200 hover:text-white mr-4 text-xl">Shopping</a>
            {% endif %}
        </div>
    </div>
</nav>


<script>
    document.addEventListener("DOMContentLoaded", function () {
        const burgerBtn = document.getElementById("burger-btn");
        const burgerMenu = document.getElementById("burger-menu");

        burgerBtn.addEventListener("click", function () {
            burgerMenu.classList.toggle("hidden");
            console.log('bouton');
        });
    });
</script>