{{ form_start(quantityForm,

    attr:
    {"action": path('app_shopping_add_to_cart', {id:product.id})
    }
)
}}
        {{ form_widget(quantityForm) }}
<button type="submit">add to cart</button>
{{ form_end(quantityForm) }}